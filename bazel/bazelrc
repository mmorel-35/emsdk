# The --incompatible_enable_cc_toolchain_resolution flag is no longer needed in Bazel 7+
# as the new CC toolchain resolution is now the default behavior.
build:wasm --platforms=@emsdk//:platform_wasm
